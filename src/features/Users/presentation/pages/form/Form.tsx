import React from "react";
import { observer } from "mobx-react-lite";
import { X } from "lucide-react";
import { UserViewModel } from "../../viewModels/viewModelUser";

interface Props {
  viewModel: UserViewModel;
}

export const UserForm = observer(({viewModel}: Props) => {

}) 